cmake_minimum_required(VERSION 3.5)

project(Gens)

add_subdirectory(src)

# We will likely need to include the pixmaps directory later so that
# the image assets can be copied to the final build location

